# AGENTS.md — 项目开发规范

本文档是 AI 助手与本仓库协作者共同遵守的开发规范，所有改动必须遵循。

## 1. 编码规范（阿里巴巴编码规范核心原则）

- **命名**：变量、函数、文件名一律使用英文命名，语义清晰、见名知义；**注释使用中文**。
- **单一职责**：每个模块/函数只做一件事，职责划分清晰，禁止大而全的“上帝函数”。
- **禁止魔法数字**：所有可调参数必须配置化，写入 `src/config/defaults.yaml`，禁止在代码中写死。
- **参数配置化**：任何运行参数（端口、超时、UA、代理、测速地址、策略组名等）都必须支持
  配置文件 / 环境变量 / 运行时 API 三种方式修改，禁止为单一场景硬编码。
- **防御式编程**：对外部输入（订阅内容、用户参数）做校验与容错，解析失败不影响整体。
- **可读性与可拓展性**：新增协议解析器只需在 `src/parsers/` 新增文件并在 `share.js` 注册前缀，
  新增目标格式只需在 `src/converters/` 新增文件并在 `index.js` 注册。

## 2. Git 规范

- commit message 使用**中文**，格式：`<type>: <描述>`。
  type 取值：`feat`（新功能）/ `fix`（修复）/ `docs`（文档）/ `refactor`（重构）/ `test`（测试）/ `chore`（杂项）。
- 禁止将任何密钥、令牌、订阅链接提交到仓库（包括 README、配置文件、示例）。

## 3. 质量检查

- 改动代码后**必须**运行全套检查：`make check`（含语法检查 + 单元测试）。
- 提交前确认 `npm test` 全部通过。
