{
  "name": "subbridge",
  "version": "1.0.0",
  "description": "通用订阅转换与节点转发服务：支持多协议解析（ss/ssr/vmess/vless/trojan/hysteria/hysteria2/tuic/clash/v2ray），输出 Clash / sing-box 等目标格式，支持 HTTP(S) 订阅抓取与上游代理转发",
  "main": "app.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "test": "node --test",
    "check": "node --check app.js && find src -name '*.js' -print0 | xargs -0 -n1 node --check && npm test"
  },
  "keywords": [
    "subscription",
    "converter",
    "clash",
    "mihomo",
    "sing-box",
    "v2ray",
    "shadowsocks",
    "proxy"
  ],
  "license": "MIT",
  "dependencies": {
    "fastify": "^4.28.1",
    "js-yaml": "^4.1.0",
    "socks": "^2.8.4",
    "undici": "^6.21.0"
  },
  "optionalDependencies": {
    "better-sqlite3": "^11.10.0"
  }
}
